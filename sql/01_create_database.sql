# Create the project database
CREATE DATABASE IF NOT EXISTS business_operations_analytics;

# Use the project database
USE business_operations_analytics;

# Confirm the active database
SELECT DATABASE();